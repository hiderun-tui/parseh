{
  "name": "parseh-mcp",
  "version": "0.1.0",
  "description": "Local MCP server exposing PARSEH V0.2 protocol primitives as tools for LLM agents.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "parseh-mcp": "./dist/src/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6"
  },
  "engines": {
    "node": ">=20"
  }
}
